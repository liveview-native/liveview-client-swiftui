targets:
  macOS:
    type: application
    platform: macOS
    sources:
      - Sources/${LVN_APP_NAME}
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: ${LVN_BUNDLE_IDENTIFIER}
    dependencies:
      - package: LiveViewNative